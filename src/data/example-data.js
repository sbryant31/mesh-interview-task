function getUser() {
  // should return a promise.
}

function getAllRecentSignups() {
  // should return a promise.
}

function updateUser(id, fields) {
  // should return a promise.
}

module.exports = {
  getUser,
  updateUser,
  getAllRecentSignups
}
